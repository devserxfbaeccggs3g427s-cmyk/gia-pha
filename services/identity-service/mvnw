#!/usr/bin/env bash
# Stub Maven Wrapper. The real wrapper jar is generated by
# `mvn wrapper:wrapper` during service bootstrap. This file exists so
# CI can copy the standard mvnw contract; replace with the
# `mvnw`/`mvnw.cmd` produced by the bootstrap.
exec mvn "$@"
