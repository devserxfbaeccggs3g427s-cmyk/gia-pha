<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.familya.identity</groupId>
    <artifactId>identity-service</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>jar</packaging>
    <name>Family Tree — Identity Service</name>
    <description>
        Identity service (reference implementation). Owns users, credentials,
        OAuth accounts, verification, lockout, opaque sessions, and revocation.
        Publishes identity events through the local outbox.
    </description>

    <properties>
        <service.name>identity-service</service.name>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.familya.platform</groupId>
                <artifactId>familya-platform-bom</artifactId>
                <version>1.0.0</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencies>

    <dependencies>
        <dependency>
            <groupId>com.familya.platform</groupId>
            <artifactId>familya-platform-spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.familya.platform</groupId>
            <artifactId>familya-platform-outbox-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.familya.platform</groupId>
            <artifactId>familya-platform-security-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.familya.platform</groupId>
            <artifactId>familya-platform-observability-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.familya.platform</groupId>
            <artifactId>familya-platform-resilience-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.familya.platform</groupId>
            <artifactId>familya-platform-grpc-starter</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>${project.artifactId}</finalName>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>com.familya.identity.IdentityServiceApplication</mainClass>
                    <image>
                        <name>ghcr.io/familya/${project.artifactId}</name>
                    </image>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.flywaydb</groupId>
                <artifactId>flyway-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.jooq</groupId>
                <artifactId>jooq-codegen-maven</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
