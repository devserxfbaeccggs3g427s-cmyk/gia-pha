server:
  port: 8082
  shutdown: graceful

spring:
  application:
    name: audit-service
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/audit?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: ${MYSQL_SVC_AUDIT:audpw}
    password: ${MYSQL_SVC_AUDIT_PASSWORD:audpw}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc.time_zone: UTC
  flyway:
    enabled: true
    schemas: audit
    default-schema: audit
    locations: classpath:db/migration
    baseline-on-migrate: true
    baseline-version: 0
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER:giapha}
    password: ${RABBITMQ_PASS:giapha}
    virtual-host: /

service:
  name: audit-service
  schema: audit
  rabbit-host: ${RABBITMQ_HOST:localhost}
  rabbit-port: ${RABBITMQ_PORT:5672}
  rabbit-user: ${RABBITMQ_USER:giapha}
  rabbit-password: ${RABBITMQ_PASS:giapha}
  outbox-poll-interval: 1s
  outbox-batch-size: 100
  publishes:
    - events.audit

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
      show-details: when_authorized

logging:
  pattern:
    level: "%5p [requestId=%X{requestId:-}]"
