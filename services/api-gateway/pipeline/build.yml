name: api-gateway
on:
  push:    { paths: [services/api-gateway/**, platform/starters/**] }
  pull_request: { paths: [services/api-gateway/**, platform/starters/**] }
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { distribution: temurin, java-version: '25' }
      - run: mvn -B -ntp -f services/api-gateway/pom.xml -DskipTests package
      - run: mvn -B -ntp -f services/api-gateway/pom.xml -Dtest=ArchitectureTest test
      - run: mvn -B -ntp -f services/api-gateway/pom.xml jib:build
      - run: cosign sign --yes ghcr.io/familya/api-gateway:${{ github.sha }}
