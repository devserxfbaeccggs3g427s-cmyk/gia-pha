package vn.giapha.research.events.domain;

/** Legacy-frozen event types (`src/data/types.ts`). */
public enum EventType {
    BIRTHDAY,
    WEDDING,
    FUNERAL,
    REUNION,
    ANNIVERSARY,
    CUSTOM
}
