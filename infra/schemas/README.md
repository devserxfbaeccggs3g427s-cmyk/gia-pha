# Schemas (AsyncAPI/JSON Schema) for events.
# Per Requirement 9.4: each event payload has a schema id and version; producers
# and consumers use a shared schema registry (the checked-in folder).
# Phase 0.2 libs/contract-types generates DTOs from these schemas.
